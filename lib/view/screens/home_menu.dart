import 'package:flutter/material.dart';

class HomeMenu extends StatelessWidget {
  const HomeMenu({Key? key}) : super(key: key);

  static Page page() => const MaterialPage<void>(child: HomeMenu());

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
